# 衡学 Study Balance — 使用说明

这是一个给你自己用的学习管理小应用：今日总览、任务清单、智能日历、负载分析、专注模式（白噪音、专注小花园）、习惯打卡、考试倒计时都在里面。有两个版本：

| | 电脑版（本机） | 网页版（GitHub Pages） |
|---|---|---|
| 怎么打开 | 双击 `启动衡学.bat` | 浏览器打开 https://didadida7747.github.io/hengxue/ |
| 数据存哪 | 本机 `data.json` 文件 | 各设备自己的浏览器里 |
| 手机电脑同步 | ✅ 同一份实时数据（同一 Wi-Fi 下） | ❌ 各自独立，用"导出/导入备份"搬家 |
| 需要电脑开机 | 手机访问时需要 | 不需要，随时随地 |

两个版本界面和功能完全一样，"我的空间"里都会标明当前用的是哪个。

---

## 一、怎么打开（电脑版）

1. 打开 `E:\a-study balance` 这个文件夹
2. **双击 `启动衡学.bat`**
3. 会弹出一个黑色窗口（这是"发动机"，别关它），然后浏览器自动打开衡学首页
4. 用完想关掉：直接关那个黑色窗口就行

> 建议把 `启动衡学.bat` 右键 → 发送到 → 桌面快捷方式，以后从桌面一点就开。

## 二、怎么在手机上用

**网页版（推荐，随时随地能用）：** 手机浏览器打开 https://didadida7747.github.io/hengxue/ ，然后菜单里选「添加到主屏幕」，以后像 App 一样点开。不需要电脑开机。

> 国内访问 GitHub Pages 偶尔会慢或打不开，等一会儿或换个小时再试属正常现象。

**电脑版（家里的局域网访问）：**

前提：**手机和电脑连的是同一个 Wi-Fi**（比如都在家里的网里）。

1. 电脑上先双击 `启动衡学.bat`，保持黑窗口开着
2. 手机打开浏览器（Safari 或 Chrome 都行），输入电脑上显示的地址
   - 地址长什么样：启动时黑窗口里有，比如 `http://192.168.1.5:3000`
   - 黑窗口里可能列了好几个地址：**先试第一个，打不开就换下一个**（不同电脑网卡不一样）
   - 忘了也没关系：打开衡学 → 底部「我的」→「我的空间」，页面里直接显示手机访问地址，还有"复制"按钮
3. 想像 App 一样点开？在手机浏览器里：菜单 → **添加到主屏幕**，以后桌面上就有"衡学"图标

**手机上的数据和你电脑上是同一份**，在手机上勾掉一个任务，电脑上立刻也能看到。

常见问题：

| 现象 | 原因和解决办法 |
|---|---|
| 手机打不开（电脑版） | ① 确认电脑黑窗口还开着 ② 确认手机和电脑连同一个 Wi-Fi ③ 第一次运行时 Windows 弹出"是否允许访问网络"，点了"允许" |
| 手机突然连不上（电脑版） | 电脑可能休眠/关机了，把电脑唤醒再试 |
| 第一次启动弹防火墙提示 | 点"允许访问"（勾选"专用网络"） |
| 网页版偶尔打不开 | GitHub 在国内时快时慢，稍等再试；重要数据记得常"导出备份" |

## 三、数据在哪里，会不会丢

- **电脑版**：所有数据都存在文件夹里的 `data.json` 文件中，**不上传到任何服务器**（GitHub 仓库里只有程序代码，没有你的数据）
- **网页版**：数据存在各设备浏览器的本地存储里；换设备用「导出备份 → 导入备份」搬家
- 担心丢失时：打开「我的空间」→「导出备份」，会下载一个备份文件；换电脑时用「导入备份」恢复
- 想清空重来：「我的空间」→「重置为示例数据」
- 电脑版还有双保险：每次保存前会自动把上一份存为 `data.json.backup`

## 四、每天怎么用（建议）

1. 早上打开衡学，看「今日总览」：今天有多少安排（学习负载）、下一节课是什么、最近的考试还剩几天
2. 在「任务清单」里把今天要做的事列出来（已逾期 / 今天 / 以后 会自动分组）
3. 有课/有固定安排就在「智能日历」里加进去（勾"每周重复"就是课表；点任何一个安排可以直接编辑）
4. 学习时点开「专注模式」，选 25/45/60 分钟或自定义时长，可以打开白噪音；每完成一个番茄钟，「专注小花园」会多种一棵树
5. 「习惯打卡」里放每天都要做的小事（背单词、阅读……），打完卡连续天数会一直涨
6. 完成一件事就打个勾，看"学习势能"涨起来

## 五、界面里的名词

- **学习负载**：今天还剩多少学习安排，0–100 分，越低越轻松
- **学习势能**：你最近的完成势头，分越高说明状态越好
- **今日节奏**：今天的时间安排表，正在进行的会标 NOW

## 六、改了代码怎么更新到网页

双击 `更新到GitHub.bat`，等它显示"全部上传完成"，一两分钟后网页就是新版了。

---

技术备查（给以后的自己）：应用由 `server.js`（Node.js 电脑版服务器）+ `public/` 网页组成；网页版部署到 GitHub 仓库 `didadida7747/hengxue`，`public/` 下的文件映射到仓库根目录；改代码不用重启服务器，刷新浏览器即可；端口自动从 3000 往后找空闲的。

